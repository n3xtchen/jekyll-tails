# frozen_string_literal: true

if ENV["IS_IN_CHINA"]
  source "https://gems.ruby-china.com/"
else
  source "https://rubygems.org"
end

# Specify your gem's dependencies in jekyll-tails.gemspec
gemspec

gem "jekyll", ENV["JEKYLL_VERSION"] if ENV["JEKYLL_VERSION"]
